package grafico;

import java.awt.Graphics;
import gui.desenho.Grafico;
import gui.desenho.PainelGrafico;

public class HexagonalMatCoresGrafico extends MatCoresGrafico implements Grafico<CMat> {
    
    @Override
    public void desenha( Graphics g, CMat cmat, PainelGrafico painel ) {
        
    }
            
}
